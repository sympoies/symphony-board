node_modules/
*.log

# Local DB and its WAL/SHM sidecars (never commit the store).
*.db
*.db-wal
*.db-shm
data/

# Secrets and local config overrides.
.env
config/sources.json

# Emitted contract artifacts (regenerate from the DB).
out/
*.contract.json

# Build output (the UI is the only package with a build step).
dist/

# Project-local Claude directory (skills live in .agents/skills/)
.claude/
