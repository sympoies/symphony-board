{
  "name": "@symphony-board/android",
  "version": "1.1.4",
  "private": true,
  "description": "Android thin-client shell for the Symphony Board read-only UI.",
  "type": "module",
  "scripts": {
    "init": "tauri android init --ci",
    "dev": "tauri android dev",
    "run": "tauri android run",
    "build": "tauri android build --ci",
    "build:apk": "tauri android build --apk --ci",
    "info": "tauri info"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.2"
  }
}
