# 2026-07

## 2026-07-12 — Bounded canonical-history Graph focus

**Result**
- Delivered a read-only focused-Graph projection that follows current canonical
  relationships through a selectable one-to-five-hop neighborhood while the
  overview remains range-windowed.
- Added deterministic cycle handling, original directed edges, and explicit
  depth / 200-node / 500-edge limit reporting, with a labeled loaded one-hop
  fallback for static or local-file clients.
- Independent review hardened the hot path to indexed frontier reads inside one
  SQLite/Postgres snapshot, coalesced identical requests, omitted unused item
  bodies, served gzip, strictly validated client graphs, and bounded force-layout
  ticks. Isolated focused items remain visible even when they have no edge.

**Why / context**
- The normal contract and `/api/range` payloads are deliberately bounded, so a
  direct-focus subgraph could not reveal older chains wholly outside the active
  item window. A separate operational read preserves the contract boundary and
  avoids inflating every page load for a viewer-local inspection.

**Evidence**
- Backend typecheck and 601/601 tests passed; UI 377/377, production build, and
  render-smoke passed. The smoke proves depth refetch, stale-response rejection,
  every limit reason, API/local-file fallback, and an older second-hop relation.
- Combined line coverage: 90.20% (floor 85%); thin desktop release build passed.
- SQLite/Postgres conformance + live e2e (61 tests) and the Postgres compose
  deployment smoke passed. Required specialist review covered testing,
  maintainability, API contract, security, performance, and red-team lenses.

**Links**
- Issue: #565
- PR: #566
- Implementation commit: aedc85f
- Design boundary: `docs/DESIGN.md` Graph and read-only diagnostics sections.

## 2026-07-10 — Live Telegram network-stall recovery

**Result**
- Validated bounded recovery for stalled snapshot bodies, SSE connection
  establishment, Telegram requests, and prolonged rapid-failure loops.
- Kept healthy idle SSE streams open indefinitely and preserved the durable
  cursor contract across reconnects and container restarts.

**Why / context**
- Exponential reconnect backoff only helps after a request returns. A peer that
  accepts a connection but never completes headers or a finite response can
  leave the bridge alive yet permanently inert. Short request bounds recover
  those attempts; a longer failure window delegates persistent faults to the
  existing container restart policy without churning healthy idle streams.

**Evidence**
- Focused stalled-header, stalled-body, Telegram timeout, and rapid-failure
  tests passed.
- `pnpm run typecheck` passed.
- `pnpm test`: 589/589 passing.
- Test-first evidence verified with a failing pre-fix reproduction and passing
  final validation.

**Links**
- Issue: #559
- PR: #560
- Implementation commit: 5b2e177

## 2026-07-02 — Live capabilities diagnostics

**Result**
- Added a read-side `GET /api/capabilities` surface for board route availability
  and Live read status (`unsupported` / `unreachable` / `empty` / `ready`).
- Settings now explains Live read availability, the latest retained Live
  sequence/time, optional non-secret webhook setup hints, and allowlist count
  without adding a second Live URL setting.
- Docker wiring keeps the browser on the existing Server URL for
  `/contract.json`, `/api/range`, `/api/capabilities`, and `/api/live*`; public
  webhook ingress remains deployment-owned and is still not proxied by the UI
  sidecar.

**Why / context**
- Hosted and thin-client users need to diagnose "Live unavailable" versus "Live
  receiver reachable but no events yet" from the same read-side base URL they
  already configure. The capability document is intentionally operational
  metadata, not a contract bump: it may report route booleans, latest Live
  sequence/time, provider names, subscribed-event hints, and allowlist counts,
  but never credential material.

**Evidence**
- `pnpm run typecheck`
- Focused capability tests: 13/13 passing
- `pnpm test`: 576/576 passing
- `pnpm --filter @symphony-board/ui run test`: 362/362 passing
- `pnpm --filter @symphony-board/ui run build`
- `pnpm --filter @symphony-board/ui run smoke`: PASS
- Test-first evidence verified complete at
  `agent-out/projects/sympoies__symphony-board/20260702-220141-issue-549-live-capabilities/test-first`.

**Links**
- Issue: #549
- Commit: 47f8bac (`feat(live): add read-side capabilities diagnostics`)

**Follow-ups**
- Deployment repositories can opt into concrete webhook setup hints by setting
  `LIVE_WEBHOOK_PUBLIC_URL` and, when useful, provider/events metadata.

## 2026-07-01 — Clear the Code Scanning backlog + drain the dependabot queue

**Result**
- Took open Code Scanning alerts from 10 → **0** across two releases, and the
  open PR queue to **0**.
- **Board image (v1.12.7, #541):** dropped build-only tooling from the runtime
  layer — the base image's bundled `npm` (its own vulnerable `undici` + `tar`)
  and the corepack `pnpm` cache — and bumped the pnpm pin 11.7.0 → 11.9.0. That
  cleared all 10 board-image alerts (bundled-`undici` ×4, `tar`, corepack-`undici`
  ×4, the `pnpm` advisory).
- **Web image (v1.12.8, #542):** upgraded `libexpat` 2.8.1-r0 → 2.8.2-r0 in the
  nginx stage, clearing the 13-CVE cluster (CVE-2026-50219 / -561xx / -564xx) a
  fresh nginx-alpine pull had surfaced.
- **Dependency queue:** consolidated 8 open npm dependabot PRs into one coherent
  lockfile update (#543) and merged 3 GitHub Actions bumps (#530/#531/#533).
  Shipped with the libexpat fix in v1.12.8.

**Why / context**
- Every Trivy *image* finding lived in base-image or build tooling the runtime
  never executes — the container entrypoint only runs `node`, so the fix was to
  stop *shipping* the tooling, not to patch an app dependency. The board image
  is single-stage and had left `npm` + the corepack cache in the final layer;
  removing them in the same install `RUN` keeps them out of the image entirely
  (and shrinks it). The web image's `libexpat` is a transitive OS package; the
  UI sidecar only serves static files (never parses XML), so exposure was
  negligible, but a pinned `apk add --upgrade "libexpat>=2.8.2-r0"` (fails
  closed) keeps the scan clean.
- The React bump *had* to be consolidated: dependabot split `react` (#537) from
  `react-dom` (#538), so each failed CI alone with a React 18/19 internals
  mismatch (`ReactCurrentBatchConfig` undefined). Bumped as a set — react +
  react-dom 19, matching `@types`, `@vitejs/plugin-react` 6, `vite` 8 — the app
  needed **no code changes**.
- Image-scan alerts only reconcile after a new `:latest` is published *and* the
  `trivy-image` job re-runs (it is `schedule`/`workflow_dispatch`-only, scanning
  the published tag). Merging a fix never clears the alert on its own — each fix
  needed a release plus a manual `security.yml` dispatch to confirm.

**Evidence**
- Local Trivy scans of the rebuilt images: board = 0 vulns (npm / corepack /
  undici / tar gone, `node` + `postgres` intact); web = 0 vulns (`libexpat`
  2.8.2-r0).
- React 19 batch: root 574/574, UI 359/359, UI + standalone `vite` 8 builds,
  and both render-smokes PASS (0 console errors / 0 uncaught exceptions — the
  exact net for the crash class); CI 12/12 required green.
- Alerts: board #11–20 → fixed after v1.12.7 + rescan; web `libexpat` #21–33 →
  fixed after v1.12.8 + rescan. Final: **0 open alerts, 0 open PRs.**
- Both releases deployed to the self-hosted production stack via its
  release-env bump PR; all board + web containers verified on the new tag and
  healthy.

**Links**
- PRs: #541 (board tooling), #542 (web libexpat), #543 (dependency batch),
  #530 / #531 / #533 (Actions bumps). Superseded & closed: #532, #534–#540.
- Releases: v1.12.7, v1.12.8.

**Follow-ups**
- Optional review-noted hardening: a dev-mode (StrictMode) render-smoke variant;
  confirm the sample contract exercises rich markdown (GFM/raw-HTML) for
  react-markdown 10; consider pinning `@types/node` to the runtime's Node-24
  line rather than tracking the latest major.
